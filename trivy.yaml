# Trivy configuration for zscaler-sdk-python
# Run: trivy fs .  (or: make security-scan)
#
# Skips local_dev/ and openapi/ (gitignored; may contain dev credentials/certs)
# and other non-committed paths to focus on the actual repo.
scan:
  skip-dirs:
    - local_dev
    - openapi
    - .git
